# LLM provider - OpenAI-compatible chat completions API
openai>=1.0.0,<2.0.0

# AWS Bedrock runtime client and default credential chain
boto3>=1.34.0,<2.0.0
 
# Vector DB (RAG layer). Chroma 1.5 bundles its native index in a
# platform wheel, avoiding the source-only chroma-hnswlib dependency.
chromadb==1.5.9
 
# SQL/PLSQL structural parsing aid
sqlglot>=25.0.0,<26.0.0
 
# Config management
python-dotenv>=1.0.0,<2.0.0
 
# Centralized YAML prompt loading (prompts/*.yaml)
PyYAML>=6.0,<7.0
 
# Frontend
streamlit>=1.38.0,<2.0.0
