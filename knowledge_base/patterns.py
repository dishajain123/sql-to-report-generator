KNOWLEDGE_DOCS = [
    {"id": "irac_standard", "text": "Under RBI IRAC norms, a loan account is classified as Standard as long as it is overdue by 90 days or less. Standard assets attract only a minimal standard provisioning charge, typically around 0.40%."},
    {"id": "irac_substandard", "text": "Under RBI IRAC norms, once a loan account is overdue between 91 and 365 days, it is classified as Sub-standard, an NPA category. Sub-standard assets typically require 15% provisioning, rising to 25% on the unsecured portion of the loan."},
    {"id": "irac_doubtful1", "text": "Under RBI IRAC norms, an account overdue between 366 and 1095 days (i.e. remained in the doubtful category up to 3 years) is classified Doubtful, split into Doubtful-1 and Doubtful-2 sub-buckets depending on how long it has stayed doubtful, with provisioning ranging roughly 25%-40%."},
    {"id": "irac_loss", "text": "Under RBI IRAC norms, an account overdue more than 1095 days (over 3 years) is classified Doubtful-3 or Loss, requiring 100% provisioning since recovery is considered unlikely."},
    {"id": "provisioning_formula", "text": "Provisioning amount is generally computed as the outstanding loan balance multiplied by the provisioning percentage applicable to the account's current asset classification bucket, with a higher percentage often applied specifically to any unsecured portion of the loan."},
    {"id": "cursor_pattern", "text": "A cursor FOR loop iterating over a loan or account table in banking code usually means the procedure is batch-processing many accounts at once, commonly to re-evaluate their overdue status and reclassify them."},
    {"id": "merge_pattern", "text": "A MERGE statement in banking procedures usually implements upsert/synchronization logic — inserting a new record if one doesn't exist for a key, or updating it if it does, often used for syncing classification or balance records."},
    {"id": "exception_others", "text": "A WHEN OTHERS exception handler is a generic catch-all error handler. In banking procedures it commonly logs the error (often to an audit table) rather than letting the transaction fail silently, and may or may not re-raise the exception."},
    {"id": "dynamic_sql", "text": "EXECUTE IMMEDIATE indicates dynamic SQL — a SQL statement built and run as a string at runtime. Its exact behavior often cannot be statically determined and should be flagged for manual review rather than guessed."},
    {"id": "audit_log_pattern", "text": "An insert into a table named like *_AUDIT_LOG or *_HISTORY after a business-data change is a compliance/traceability mechanism, recording who/what/when for regulatory audit purposes — common in banking systems bound by RBI or similar regulatory requirements."},
    {"id": "select_into_pattern", "text": "A SELECT ... INTO statement at the start of a procedure is fetching the current state of a business entity (e.g. a loan account's balance and overdue days) needed to make a downstream business decision."},
    {"id": "update_classification_pattern", "text": "An UPDATE statement that sets a classification or status column based on prior conditional logic represents applying the business decision made earlier in the procedure — persisting the newly determined state."},
    {"id": "trigger_pattern", "text": "A trigger firing on INSERT/UPDATE/DELETE of a business table typically enforces a business rule automatically and immediately whenever that kind of data change happens, without requiring an explicit procedure call."},
    {"id": "view_pattern", "text": "A view joining/filtering multiple banking tables usually represents a pre-defined business-relevant slice of data, such as 'all currently overdue accounts' or 'accounts due for reclassification this cycle.'"},
    {"id": "interest_accrual_pattern", "text": "Interest accrual logic typically calculates interest owed based on principal, rate, and elapsed time, and is a distinct business concern from NPA classification even though both operate on the same loan account data."},
]
